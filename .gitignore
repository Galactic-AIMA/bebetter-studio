node_modules/
dist/
.env
server/.env

# Generated videos
output/

# OAuth credentials & tokens
credentials/
server/credentials/

# n8n API key & instance info (secretos — nunca versionar)
.n8n-key
info-n8n.md

# Pinterest-synced images (local, regenerable)
data/images/
data/pinterest-sync.json
data/gallery-dl-archive.txt

# Runtime data (generated content records & usage counters)
data/videos.json
data/images-output.json
data/images-usage.json
data/phrases.json
data/images-metadata.json
data/phrases-metadata.json
data/images-metadata2.json
data/phrases-metadata2.json

# Audio de fondo (pistas locales descargables — no versionar; el README sí)
data/audio/*
!data/audio/README.md

# Registro de actividad (logs de runtime)
data/logs.jsonl

# SQLite database (runtime, no versionar)
data/bebetter.db
data/bebetter.db-shm
data/bebetter.db-wal
data/bebetter.db.bak-*

# Backups de workflows n8n generados por los scripts de mantenimiento.
# Cubre las dos formas que generan los scripts: wf_<id>_<motivo>_backup_<ts> y wf_backup_<motivo>_<id>_<ts>.
# (el patrón antiguo era solo `wf_*_backup_*.json` y dejaba pasar toda la familia `wf_backup_*`)
server/scripts/wf_*.json
server/scripts/__pycache__/

# Lockfile
package-lock.json

# Claude Code local config
.claude/

*.log
.DS_Store
Thumbs.db
server/scripts/add-facebook-reels.py
server/scripts/enrich-notifications.py
server/scripts/fb-only-test.py
server/scripts/fix-ig-dup-and-hashtags.py
server/scripts/fix-preview-gate-wiring.py
server/scripts/wf_pub_MODIFIED.json
# Scripts de mutación de n8n del agente + copies (se corren con --apply)
server/scripts/create-agent-workflow.py
server/scripts/fix-agent-proyectar.py
server/scripts/rebot-bebetter.py
server/scripts/update-copy-prompts.py
server/scripts/backup-copy-prompts.py
server/scripts/copy_prompts_backup.txt
# Scheduler de carruseles en n8n (se corre con --apply)
server/scripts/add-carousel-scheduler.py
server/scripts/fix-igtoken-sheet-schema.py
server/scripts/add-carousels-to-agent.py

# --- Scripts de un solo uso y datos crudos (2026-08-02) ---
# Criterio: al repo va lo que se vuelve a ejecutar o lo que no tiene otra copia.
# Fuera queda lo que ya cumplió su función (mutaciones de n8n ya aplicadas, migraciones
# ya corridas) y los volcados de APIs, que se regeneran llamando otra vez a la fuente.

# Mutaciones puntuales de n8n, ya aplicadas al workflow en producción
server/scripts/add-retries.py
server/scripts/fix-carousel-token-filter.py
server/scripts/test-carousel-item.py

# Migraciones de un solo uso, ya ejecutadas contra la DB
server/scripts/fix-media-type.ts
server/scripts/importar-frases-dostiempos.ts
server/scripts/analizar-test-registro-propio.ts
server/scripts/publish-now.ts

# Recolectores y analizadores de Apify (se rehacen desde el scraper; el crédito
# se renueva por aniversario de cuenta, próxima ventana 2026-08-29)
server/scripts/apify-*.py
server/scripts/nicho-percepcion.py
server/scripts/propio-analisis.py

# Volcados crudos de APIs (~25 MB) y resultados intermedios regenerables
server/scripts/apify_*.json
server/scripts/nicho_percepcion*.json
server/scripts/analisis_test_registro_propio.json
server/scripts/auditoria_frases_v1_defectuosa_*.json

# Mapas intermedios de las tandas de reconversión: se regeneran resolviendo por
# texto contra la DB, y sólo valen para la corrida que los creó.
server/scripts/_*.json

# Scripts de reconversión ya ejecutados (2026-08-02/03). Lo que hicieron y por qué
# está en el vault; el resultado está en la DB. Volver a correrlos sobre el banco
# actual no tendría sentido: buscan textos que ya no existen.
server/scripts/aplicar-tanda2.ts
server/scripts/aplicar-tanda3.ts
server/scripts/aplicar-tanda5.ts
server/scripts/reparar-vinculo-tanda2.ts

# EXCEPCIONES — esto NO es un script de un uso y no tiene otra copia versionada
# (la DB está gitignoreada, así que el repo es el único respaldo):
#   - frases-reescritas-dostiempos.md : las 20 frases escritas A MANO por David
#   - auditoria_frases.json           : insumo de la columna `estructura` del banco
#   - auditar-frases-dostiempos.py    : clasificador, se reusa al reconvertir frases
!server/scripts/auditoria_frases.json
